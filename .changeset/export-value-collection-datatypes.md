---
'json-edit-react': minor
---

Export the `valueDataTypes` and `collectionDataTypes` constants (the scalar and collection subsets of the already-exported `standardDataTypes`). Useful for restricting `allowTypeSelection` to primitives-only or collections-only.
