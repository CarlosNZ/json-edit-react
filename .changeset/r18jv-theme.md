---
'@json-edit-react/themes': minor
---

Add the `r18jvTheme` — a light theme that ships its own icon glyphs via `Theme.icons`, demonstrating a theme whose look includes its own SVG glyphs (not just colours). It also uses a `property` style function to colour array-index keys like numbers while object keys stay green.
