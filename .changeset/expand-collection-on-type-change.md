---
'json-edit-react': minor
---

Changing a value's type to `object` or `array` now launches the new collection expanded (#217). A level-based `collapse` setting would previously leave the just-created collection collapsed, hiding its contents until manually expanded.
