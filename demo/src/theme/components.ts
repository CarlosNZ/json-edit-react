// import { theme as base } from '@chakra-ui/react'

const components = {
  Heading: {
    baseStyle: { color: 'brandDark.800' },
  },
  Input: {
    // Darken border, fill inside when not focused
  },
}
export default components
